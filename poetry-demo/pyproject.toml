[tool.poetry]
name = "poetry-demo"
version = "0.1.0"
description = ""
authors = ["melany estevez camata <bilany@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
scrapy = "^2.12.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
